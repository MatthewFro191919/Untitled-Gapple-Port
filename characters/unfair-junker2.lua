local healthtolower = 0.02;

function opponentNoteHit(membersIndex, noteData, noteType, isSustainNote)
    if dadName == 'unfair-junker2' then
        setProperty('health', getProperty('health') -(healthtolower / 12))
    end
end
